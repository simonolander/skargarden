name: Build project

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: simonolander/skargarden-docker-image@v1
      run: |
        yarn install